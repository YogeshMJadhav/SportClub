import React from 'react';
import { Button } from 'react-bootstrap';

export default function ActionBar() {
    return(
        <div>
            <Button >
                Edit
            </Button>

            <Button>
                Delete
            </Button>
        </div>
    )
}